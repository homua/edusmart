
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULE FOR DEBUGGING: Allow all reads and writes to all collections.
    // This is insecure and should be replaced with proper rules once the issue is resolved.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
